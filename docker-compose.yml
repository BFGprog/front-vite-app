version: "3.8"

services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - "9002:9002"
    restart: unless-stopped
    networks:
      - backend

networks:
  backend:
    external: true
    name: project_backend
